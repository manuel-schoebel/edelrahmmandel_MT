apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: edelrahmmandel-data
  namespace: edelrahmmandel-staging
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: hcloud-volumes
  resources:
    requests:
      storage: 10Gi
